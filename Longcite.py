
path_model=r'/data/@Zilong_Works/Data/SourceData/Model_Weight_Cache/Reasoning_LLM/LongCite/LongCite-llama3.1-8b'

# # Use a pipeline as a high-level helper
# from transformers import pipeline

# pipe = pipeline("text-generation", model=path_model, trust_remote_code=True)
# pass

# # Load model directly
# from transformers import AutoTokenizer, AutoModelForCausalLM

# tokenizer = AutoTokenizer.from_pretrained(path_model, trust_remote_code=True)
# model = AutoModelForCausalLM.from_pretrained(path_model, trust_remote_code=True)
# pass

import json
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM

tokenizer = AutoTokenizer.from_pretrained(path_model, trust_remote_code=True)
model = AutoModelForCausalLM.from_pretrained(path_model, torch_dtype=torch.bfloat16, trust_remote_code=True, device_map='auto')

context = "Citation is essential to the creation and propagation of knowledge and is a well-understood part of scholarship and scientific publishing. Citations allow us to identify the cited material, retrieve it, give credit to its creator, date it, and provide partial knowledge of its subject and quality.  The citation graph, or citation network, is a model used to describe how citations link research entities, typically papers, journals, and books (Harzing & Van der Wal, 2008; Tang et al., 2008). It enables a number of important activities such as the following:  • Exploration of the graph to find publications of interest. • Tracking of authorship of papers: Citing and following citations is one way to attribute credit to authors and to keep up to date with the work of others. • Dissemination of research findings: The exploration of citations and cited authors enables the dispersed communities of researchers to share their findings and engage in discussions. • Computation of bibliometrics for the analysis of one researcher, venue, or publication impact in particular fields. The citation graph is the basis for nearly all the currently used bibliometrics, such as impact factor and h-index. Throughout this paper, we refer to an idealized “citation graph” as though it were a real and unique digital artifact that represents papers and the citations between them. Of course, it is not unique: Various organizations have distinct implementations of it. Among these, we count: Google Scholar, the Microsoft Academic Graph (MAG)1, the Open Academic Graph (OAG) (Tang et al., 2008), Semantic Scholar (SS)2, AMiner (AM)3, and PubMed4 (this is more a linked collection of documents than a full-fledged citation graph), Scopus5, and the Web of Science6. These graphs differ in many aspects, such as their coverage, their being open- or closedaccess, and their schema; but in all of these, the basic structure is a directed graph, in which the vertices represent publications and the edges represent citations from one publication to another (Price, 1965).  Most of the information about papers is contained in annotations of the nodes. The edges are generally typed but not annotated (an exception is MAG, which carries context, as we discuss later). Although in early models, nodes only represented papers and the only edges were “cites” edges, recently, citation graphs have been extended with richer information (Peroni & Shotton, 2020). These extensions may carry author nodes with a “wrote” edge to papers, journal/conference nodes with a “part of” edge from papers, and subject nodes with the corresponding edges. Although representations differ, the purpose is similar: to provide the services described above.  1.2. The Need for Data Citation  Scientific publications increasingly rely on curated databases, which are numerous, “populated and updated with a great deal of human effort” (Buneman, Cheney et al., 2008), and at the core of current scientific research7. In this context, references to data are starting to be placed alongside traditional references. Hence, there has been a strong demand (FORCE-11, 2014; CODATA-ICSTI Task Group on Data Citation Standards and Practices, 2013) to give databases the same scholarly status as traditional scientific works and to define a shared methodology to cite data. Scientific publishers (e.g., Elsevier, PLoS, Springer, Nature) have taken up data citation by instituting policies to include data citations in the reference lists.  The open research culture (Nosek, Alter et al., 2015) is based on methods and tools to share, discover, and access experimental data. Moreover papers, journals, and articles should provide access to all the data that they use (Cousijn, Feeney et al., 2019). Researchers and practitioners (e.g., journalists and data scientists) who make use of electronic data should be able to cite the relevant data as they would cite a document from which they had extracted information (Cousijn, Kenall et al., 2017; Nature Physics Editorial, 2016). As we shall see, the citation graphs can become a fundamental tool in the pursuit of the goal of accessibility and networking between papers and data.  We also observe that data occupy a crucial role today in research, emerging as a driving instrument in science (Candela, Castelli et al., 2015). Data citations should be given the same scholarly status as traditional citations and contribute to bibliometrics indicators (Belter, 2014; Peters, Kraker et al., 2016). Principles such as Findability, Accessibility, Interoperability, and Reusability (FAIR) (Wilkinson, Dumontier et al., 2016) require data to be easily findable and accessible, qualities that are more readily available once data can be appropriately cited. In this sense, we can say that the FAIR principles encourage the adoption of data citation.  The reasons given for data citation are the same those given for a conventional citation (CODATA-ICSTI Task Group on Data Citation Standards and Practices, 2013): recognition of the source (e.g., a title); credit for the author, curator, or agent; establishment of its currency (when it was created); where it was located; and how it was extracted. The last three of these fall under the general heading of provenance and are important when one wants to reproduce some analysis on the data or establish the trustworthiness of a claim.  Data sets and databases are usually more complex and varied than textual documents, and they introduce significant challenges for citation (Silvello, 2018). Text publications have a fixed form, do not change over time, are interpretable as independent units, share a standard format and representation model, and are composed of predetermined, albeit domaindependent, sets of elements that are considered as citable (e.g., the whole paper or book or a chapter). Scientific databases are structured according to diverse data models and accessed with a variety of query languages. What can be cited may range from a single datum to data subsets or aggregations specified by the person or agent that extracts the relevant data, and deciding a priori what can and cannot be cited is rarely feasible. Data citation introduces multiple citation types, besides the classical papers citing papers. These are papers citing data, data citing papers, and data citing data.  1.3. Data Citation in the Citation Graph  Our purpose in this paper is to discuss whether, in its current form, the current model of the citation graph can properly accommodate data citation. We claim that, despite all the features and modifications that have been added to various implementations of the citation graph, at least two significant features are generally missing or poorly represented. These shortcomings already limit what we can represent with existing implementations, and we argue that they make impossible the proper representation of data citations.  The first shortcoming concerns the assignment of credit when a referenced scientific work is corrected or augmented with another version. A typical example case is that of a preprint paper that gets cited before its peer-reviewed version is published. It is common for the authors to prefer that the preprint citations are merged with those of the peer-reviewed version. Something similar happens also when an updated version of a data set is published.  In the case of data, we need to consider that a database may be composed of multiple independently citable parts (e.g., a single record, a table, a view). Every single citable part can evolve and change over time and obtain citations (also views or downloads, when monitoring other scientometrics signals) at a different point in time. Therefore, it can be necessary to aggregate these statistics over all the versions of the same part to measure its impact and that of the database. The MAG and S2ORC databases have also an explicit notion of multiple versions of a paper, for example preprints and final published versions. It is however uncommon to “move” citations from one version to another, following some criteria or algorithm to correctly allocate citations. Yet, aggregating citation to a single version of a scientific work would have, among other things, the desirable effect of allowing proper evaluation of the impact of the work."

query = "Introduce me what the cite graph is, and what is the value of it, and the usage of it"

result = model.query_longcite(context, query, tokenizer=tokenizer, max_input_length=128000, max_new_tokens=1024)


print("Answer:\n{}\n".format(result['answer']))
print("Context (divided into sentences):\n{}\n".format(result['splited_context']))

print("Statement with citations:\n{}\n".format(
  json.dumps(result['statements_with_citations'], indent=2, ensure_ascii=False)))


pass